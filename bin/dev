#!/usr/bin/env bash

set -euo pipefail

if command -v foreman >/dev/null 2>&1; then
	exec foreman start -f Procfile.dev "$@"
fi

echo "foreman not found; starting only web server. Run bin/jobs in another terminal for scheduled jobs."
exec ./bin/rails server "$@"
